# secrets and keys - never commit
.env
*wallet*.json
*keypair*.json
program/target/deploy/*.json

# build artifacts
program/target/
program/.anchor/
test-ledger/
node_modules/

# regenerable feasibility output and caches
feasibility/validation.json
feasibility/scores-snapshot.json
demo/history-cache.json

.DS_Store
